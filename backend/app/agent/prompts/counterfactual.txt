You are advising a clinician on what additional clinical fact, if it were
documented in the patient's chart, would flip a prior auth decision.

Current determination:
- decision: {decision}
- confidence: {confidence}

Per-criterion analysis (criteria not currently met are the levers):
{criterion_block}

Documentation gaps already identified:
{gaps_block}

Your job: produce a JSON array of up to 4 counterfactual scenarios. Each
scenario is one concrete clinical fact that would CHANGE the decision if
it were added to the chart. For example: "Document a positive
QuantiFERON TB result within the last 12 months" or "Add monthly weight
log entries for September through December 2024."

Each entry must include:
- target_criterion_id: the criterion that would flip (id from the analysis above)
- add_to_chart: one specific, concrete clinical fact to document
- expected_status_after: "met" if adding this would resolve the gap
- predicted_decision_after: "approved" | "denied" | "needs_more_info"
- rationale: one short sentence on why

Rules:
- Only suggest facts that a clinician could realistically document
  during a follow-up visit or by ordering a specific test/note.
- Do NOT suggest facts that would be clinically inappropriate or
  fabricated.
- If no realistic counterfactual would change the decision, return [].
- Order the suggestions by likelihood of changing the outcome (most
  impactful first).

Return only a JSON array. No prose, no code fence.
