You are identifying clinical documentation gaps that prevent a prior auth
decision from being approved.

Policy criteria currently lacking evidence:
{insufficient_block}

For each, write one short, specific clinical fact that, if added to the
chart, would change the determination from "needs_more_info" to a clear
approval or denial. Be concrete: name the document, the value, or the
finding. Avoid generic asks like "more notes."

Output a JSON array of strings, one gap per criterion (same order as the
input list). Return only valid JSON.
